# This package is stable but pending redesign in a future release.
# It provides resonator-vs-temperature fitting: funcs (fit functions),
# fitter (fitting routines), guess (initial parameter estimation),
# data_io (DataFrame I/O), and plot (matplotlib visualizations).
# Tests are intentionally omitted until the redesign is complete.
