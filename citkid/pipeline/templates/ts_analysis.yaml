### ----------------------------- ###
###         Analysis steps        ###
### ----------------------------- ###

ANALYSIS_STEPS:
  1 : 
    task: make_fr_spans 
    # global step. Analysis step because inputs (fres_all, qres_all) might be 
    # generated by user-defined parameters in the future. Is that sensible?
  2 : 
    task: fit_gain
    params:
      span_mult: 1 # I will implement this soon, multiplies all spans by this factor
  3 :
    task: fit_iq_circle 
    params:
      circ_mask: null # null will build a mask that is all True.
  4 : 
    task: get_idx_t
  5 : 
    task: get_theta_phase_offset 
    # is this really analysis if it doesn't take user-defined parameters? But 
    # there is a good chance we might add user-defined parameters later.
    # User param should allow user to choose between idxt or offres -> pi
  6 :
    task: get_xcal_mask 
    params:
      xcal_idx0_offset: 3 
      xcal_idx1_offset: 9
      xcal_std_cutoff: 16.0 
  7 :
    task: fit_x_theta 
    params:
      poly_x_deg: 3