### ----------------------------- ###
###         Analysis steps        ###
### ----------------------------- ###

ANALYSIS_STEPS:
  1 : 
    task: make_fr_spans 
    # global step. Analysis step so inputs (fres_all, qres_all) could be 
    # generated by user-defined parameters in the future. 
  2 : 
    task: fit_gain
    params:
      span_mult: 1 # Multiplies all spans by this factor
  3 : 
    task: fit_iq
    params:
      iq_mask: null # null will build a mask that is all True